apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-test
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: dev.mineco.gob.gt
    http:
      paths:
      - path: /test
        backend:
          serviceName: service-payara
          servicePort: 80